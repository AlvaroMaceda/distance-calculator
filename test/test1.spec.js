'use strict';

const expect = require('chai').expect;

describe('User module', () => {
    describe('tests up', () => {
        it('mocha hello world', () => {
            expect(true).to.equal(true);
        })
    })
});